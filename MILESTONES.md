# 마일스톤

**순서의 원칙: 편집기가 먼저다.** 이전 시도는 엔진을 다 만들고 편집기를 마지막에 붙이려다
«문법만 바뀐 nanDECK» 이 됐다. 이번에는 v0.1 부터 화면에서 카드를 만든다.

각 버전은 그 자체로 쓸 수 있는 상태로 끝난다.

---

## 1차 릴리스 (2026-08-02) — 기능 마무리

**«브라우저만 열고 카드를 만들어 인쇄·공유할 수 있다»** 까지 왔다.
v0.1 ~ v0.4 의 편집기 기능과 v0.7 의 PNG 가 들어갔다.

만들기 — 레이어(이미지·글자·도형·그늘) · 컴포넌트/인스턴스 · 스냅 · 되돌리기 ·
알파 인식 선택 · 눈금자 · 화면 실물 크기 보정 · 글꼴 직접 넣기 · 키워드
내보내기 — 인쇄/PDF(양면 맞물림·재단선·맞붙이기) · PNG(카드 한 장 / 덱 전체 격자)
저장 — `project.json` + 브라우저 저장소(그림·글꼴), 자동 저장
배포 — GitHub Pages (`main` 에 밀면 확인 → 빌드 → 배포)

정리한 것 — 안 쓰던 의존성(`pdf-lib`, `@pdf-lib/fontkit`) 제거 → **의존성은 셋뿐**
(react · react-dom · zustand). 죽은 코드 제거. `npm run check` 로 확인 79개.

사용자가 직접 확인한 것 — 글꼴 불러오기가 PNG·인쇄까지 반영됨 · PNG 내보내기 정상

**다음 판의 문 둘**
1. **도련** — 꺼뒀다. 위 v0.3 항목 참조
2. **실물 인쇄 검증** — PDF 안에서 잰 수치는 맞지만 아직 종이로 안 뽑아봤다.
   v0.3 의 완료 기준이라, 통과해야 «인쇄가 된다» 고 말할 수 있다

미룬 것 — 표 대량 편집(v0.2.5) · 여러 개 선택 · 복사/붙여넣기 · 카드별 뒷면 ·
룰북 · 보드판 · PPTX

---

## 2차 (2026-08-02) — 공유용 묶음 ✅

**«한 파일로 건네면 그대로 열린다».**
초기 보드게임은 만든 사람이 뽑고 다듬는 일이 대부분이라, 다른 컴퓨터로 옮기는 것이
지금 유일하게 막혀 있던 곳이었다 (그림·글꼴이 브라우저 저장소에만 있었다).

- [x] **ZIP 읽고 쓰기** (`core/zip.ts`) — 라이브러리 없이. 압축 없이 «저장» 만 한다.
      넣을 게 이미 압축된 것들(PNG·woff2)이라 다시 압축해도 몇 %도 안 준다.
      만든 시각을 안 넣어 **같은 내용이면 같은 파일**이 나온다(비교할 수 있게).
      Windows 자체 `Expand-Archive` 로 풀리는 것까지 확인
- [x] **`.handeck` 묶음** (`core/bundle.ts`) — `project.json` + `bundle.json`(목록) +
      `assets/<id>` + `fonts/<id>`. **파일 이름이 곧 내용 해시**라 짝맞추기·중복 제거가
      저절로 된다. 넣을 때 다시 해시해보면 내용이 바뀌었는지도 그 자리에서 드러난다
- [x] **글꼴 이름 충돌 처리** — id 는 안 부딪히지만 이름은 부딪힌다.
      받는 쪽에 같은 이름의 다른 글꼴이 있으면 번호를 붙이고 **그 이름을 보던
      글자 레이어까지 같이 고친다**
- [x] 열기는 `.handeck` 과 `.json` 을 **내용 앞 네 바이트로 가른다** (이름은 바뀌기 쉽다)
- [x] 칩 다듬기 — 칩이 본문 줄보다 크면 **그 줄만 밀려** 줄간격이 어긋난다.
      글씨를 `.86em` 으로 줄이고 칩 위아래를 글자에 맞췄다(높이 ≒ 1.12em < 줄간격 1.4).
      «칩(테두리)» 추가 — 테두리를 `border` 로 그리면 상자가 커지므로 안쪽 그림자로 그린다

- [x] **자유 크기 · 정사각 토큰** — 규격 목록에 «자유 크기» 와 토큰 네모(25·35·45)를 넣었다.
      지금 크기와 같은 프리셋이 있으면 그게 골라져 보인다 (전에는 늘 «직접 입력» 이라
      뭘 쓰는지 알 수 없었다). **지름을 한 변으로 쓰므로 나중에 원형으로 바꿔도 크기가 그대로다**
- [x] **인쇄 묶기 🔗** — 여러 덱을 한 종이에 이어 깐다. 덱마다 따로 뽑으면 마지막 장이
      늘 반쯤 비는데, 묶으면 그 빈 칸을 다음 덱이 채운다. **규격이 같은 덱만** 묶인다.
      조판이 «덱 하나» 전제였어서 **칸이 자기 앞면·뒷면을 들고 가도록** 고쳤다
      (`imposeDecks`) — 한 장 안에 여러 덱의 틀이 섞이기 때문이다

> ⚠️ `downloadBlob` 을 `png.ts` 에 두면 안 된다. 거기엔 `react-dom/server` 가 딸려 있어
> (+75KB) 내려받기 한 줄 때문에 주 번들이 219 → 300KB 가 됐다. `core/download.ts` 로 뺐다.

---

## 3차 (2026-08-05) — 보드 ✅

**«판도 여기서 만든다».** 머리말에 **덱 / 보드** 탭이 생겼다.
지금까지는 카드만 만들 수 있어서 판·참조표는 다른 도구로 나가야 했다.

- [x] **보드 = 한 장짜리 큰 인쇄물** (`Board`, `project.boards[]`) — 인스턴스가 없다.
      보드는 틀이 곧 결과물이라 «같은 틀로 100장» 이 필요한 물건이 아니다.
      **캔버스·레이어·키워드·글꼴·PNG 는 덱과 똑같은 것을 쓴다** —
      `componentId()` 만 탭에 따라 갈리게 해서, 편집 경로를 두 벌로 만들지 않았다
- [x] **A3** — 종이 목록에 A3 세로/가로를 넣었다 (`SHEET_PRESETS`). **덱에도 적용된다.**
      `@page` 는 원래부터 `sheet.w/h` 를 그대로 쓰고 있어서, 목록에 넣는 것만으로
      A3 를 고르면 A3 로 나간다. 목록을 `<option>` 에 박아두지 않고 상수로 뺐다 —
      보드 쪽에 A3 를 넣으려면 같은 목록을 또 적어야 했다
- [x] **판과 종이는 별개다.** A3 판을 A3 종이에 통째로(`single`), 또는 A4 여러 장에
      나눠 뽑아 이어 붙인다(`tile`). 집 프린터는 대개 A4 까지라 **둘 다** 있어야 반쪽이 아니다
- [x] **타일링** (`core/tile.ts`) — `impose.ts` 와 방향이 반대다. 저쪽은 «작은 조각을
      깔고 자르는» 문제, 여기는 «큰 한 장을 나눠 뽑아 붙이는» 문제라 격자·도련·재단선이
      없고 대신 **겹침(풀칠 여유)** 이 있다. 장수를 먼저 정하고 **겹침을 고르게 펴서**
      모든 장이 꽉 차게 한다 (안 그러면 마지막 장이 손톱만큼만 차서 «거의 빈 종이» 가 나온다)
- [x] 붙일 자리는 **점선**(`.glue`), 자를 자리는 **실선**(`.cut`) — 헷갈리면 판을 잘라버린다
- [x] 판 전체를 한 번만 그려놓고 **창으로 잘라 본다** — 장마다 다시 그리면
      이음매에서 글자 굵기·색이 미묘하게 갈린다

> ⚠️ **아직 실물로 안 뽑아봤다.** 계산과 미리보기까지다. v0.3 의 «실물 인쇄 검증» 과 같은 자리.
> 특히 **A3 는 프린터가 A3 를 지원해야** 나온다 — 인쇄 대화상자에서 종이를 A3 로 고르는
> 것까지가 사용자 몫이라 미리보기가 그 문구를 띄운다.

## 4차 (2026-08-14) — 룰북 ✅

**«설명서도 여기서 만든다».** 머리말 탭이 **덱 / 보드 / 룰북** 셋이 됐다.

- [x] **룰북 = 여러 쪽짜리 인쇄물** (`Rulebook`, `project.rulebooks[]`) —
      구조는 덱과 같다. 쪽 하나가 인스턴스 하나이고 틀은 모든 쪽이 공유한다
      (머리말·쪽번호를 한 번만 잡으면 20쪽이 따라온다). 다른 건 **수량이 늘 1** 인 것뿐이다.
      캔버스·레이어·키워드·글꼴·PNG 는 덱과 **똑같은 것을 쓴다**
- [x] **본문 레이어** (`kind: 'md'`) — 소제목·목록·표·인용이 섞인 **문서 한 덩어리**를
      상자 하나에 흘린다. 글자 레이어로 쪼개면 문장 하나 고칠 때마다 아래 상자를 전부
      다시 잡아야 한다. 치수는 전부 **본문 글씨 기준(em)** 이라 크기 하나로 통째로 줄어든다
- [x] **마크다운 파서** (`core/markdown.ts`) — 라이브러리 없이. 룰북에 실제로 쓰는 것만
      (헤딩·목록·표·인용·굵게·코드). 조판은 여전히 브라우저가 한다(원칙 3)
- [x] **원고 가져오기** (`core/rulebook.ts`) — 마크다운·JSON → 쪽 + 틀.
      **가져오기가 곧 첫 편집이다** — 20쪽짜리를 상자부터 놓아 만들게 하면 이 도구가
      없애려던 루프가 그대로 돌아온다. 규칙 문서는 어차피 마크다운으로 쓰고 있다
- [x] **중철 조판** (`core/booklet.ts`) — 8쪽이면 종이에 8·1 / 2·7 / 6·3 / 4·5 로 깔린다.
      사람이 손으로 만들면 8쪽까지는 되고 16쪽부터 반드시 틀리는 표다. 4의 배수가
      아니면 백지로 채운다. 접는 선은 **점선**(자르는 선과 달라야 한다 — 헷갈리면 두 동강)
- [x] **넘침 경고** (`bodyFit`) — 상자를 넘긴 글은 화면에서 그냥 «안 보일» 뿐인데
      종이에서는 잘려 나간다. 쪽 목록·속성 패널에 미리 띄운다. 글자 수 어림이라
      경계에서는 틀릴 수 있고, 그래서 8% 여유를 두고 «여기부터 보라» 로만 말한다
- [x] 예제 프로젝트를 **실제 작업본(`samaksil.handeck`)으로 교체** — 그림이 들어간 19덱 435장.
      예제 룰북 20쪽(`example/rulebook.md`)도 같이 들어갔다

> ⚠️ **아직 실물로 안 뽑아봤다.** 계산과 미리보기까지다 — 3차의 보드와 같은 자리.
> 특히 **양면 넘김 축**은 기종마다 달라서, 중철 한 장을 뽑아 접어보고
> 「짧은 쪽/긴 쪽」을 바꿔봐야 한다.

## v0.0 — 내보내기 스파이크 ✅ 2026-07-31

**서버 없이 인쇄용 PDF 가 나오는가.**

- [x] 인쇄 대화상자 방식으로 뽑아 **PDF 내용 스트림을 직접 뜯어 치수 측정**
- [x] 판정: **된다. 무서버 확정.** 나머지 두 방법(래스터·벡터 직접 그리기)은 볼 필요 없었다

핵심은 **페이지 상자를 조각 크기로 잡지 않는 것**. A4 로 고정하고 mm 좌표로 깔면
위치 오차 최대 0.085mm, 3칸 누적 0.043mm. 글자도 벡터로 남는다.
자세한 수치는 [`docs/print-findings.md`](docs/print-findings.md).

## v0.1 — 끌어서 카드 만들기

**목표: 코드 한 줄 안 쓰고 카드 한 장을 만들어 PDF 로 뽑는다.**

- [x] 브라우저에서 열리는 편집기 (정적 웹앱)
- [x] 캔버스에 카드 한 장 (mm 기준, 화면 배율)
- [x] 레이어 추가: 이미지 · 글자 · 도형 · 그늘
- [x] 끌어서 이동, 모서리로 크기 조절, z 순서 (목록에서 끌어 재배치)
- [x] 속성 패널 (좌표 · 크기 · 글꼴 · 색 · 정렬)
- [x] 이미지 업로드 → IndexedDB (내용 해시로 중복 제거)
- [x] `project.json` 저장 / 불러오기 — **다만 File System Access API 가 아니라
      내려받기 + 파일 고르기**. 덮어쓰기가 안 되고 매번 새 파일이 된다
- [x] **PDF 내보내기** — 63×88mm 가 62.979×87.85~88.12mm 로 나온다

**완료** — 브라우저만 열고 카드를 만들어 인쇄까지 된다.

## v0.2 — 컴포넌트와 인스턴스

**이 단계가 이 도구의 정체성이다.**

- [x] 레이어에 «카드마다 다르게» 표시 (`override: text | image`)
- [x] 인스턴스 만들기 · 복제 · 삭제
- [x] 인스턴스 선택하면 오른쪽에서 그 카드의 글자·그림을 직접 편집
- [x] 베이스를 고치면 인스턴스 전부에 반영
- [x] 인스턴스별 수량
- [x] 덱 전체 미리보기 — 왼쪽 썸네일 + 인쇄 미리보기
- [x] 시트 배치 + 재단선 (`core/impose.ts`)
- [ ] `expect` 편집 UI — 모델·경고 표시는 있는데 값을 넣을 자리가 없다
- [ ] 인스턴스 순서 바꾸기

**완료 기준** — 거의 충족. 남은 건 위 두 개.

## v0.2.5 — 표로 대량 편집  ← **미룸 (2026-08-02, 사용자 결정)**

> 편집기 자체의 손맛을 먼저 다듬기로 했다. 표는 그 다음이다.

- [ ] 인스턴스를 표로 보기 (열 = 오버라이드 레이어, 행 = 인스턴스)
- [ ] 스프레드시트에서 붙여넣기 · CSV 가져오기
- [ ] 캔버스 편집과 같은 데이터를 본다

**완료 기준** — 능력 카드 7종 100장을 표에 붙여넣어 만든다.

## v0.3 — 뒷면과 양면  (실물 검증만 남음)

- [x] 덱 공통 뒷면 — 판 위 `앞면 / + 뒷면` 탭. 앞면과 같은 규격으로 빈 틀을 만든다
- [x] 양면 인쇄 배치 (앞뒤 맞물림) — 앞·뒤·앞·뒤 순서, 뒷면 격자 좌우 뒤집기.
      긴 쪽/짧은 쪽 넘김 둘 다. 검증됨: 앞 `[1,2,3,4,5,6]` → 뒤 `[2,1,4,3,6,5]`,
      12장 → 4쪽(앞·뒤·앞·뒤)
- [x] 덱끼리 뒷면 공유 — 규격이 같은 뒷면만 목록에 뜬다 (섞이는 덱은 뒷면이 같아야 한다)
- [ ] 카드별 뒷면 (지금은 덱 공통만)
- [x] 원형 조각 (토큰) — 규격 프리셋 + 원형 렌더
- [ ] ⚠️ **도련은 꺼뒀다 (2026-08-02).** `outer` 로 뽑아보니 도련이 붙는 게 아니라
      **카드가 통째로 커져 보인다.** 짐작: ① 조각을 늘리면 판이 커지는데 가장자리에 닿지
      않은 레이어는 자리만 밀려 그 3mm 가 배경색 테두리로 남고 ② 재단 표시가 종이
      가장자리에만 짧게 그어져 **가운데 칸은 어디가 잘릴 자리인지 화면에서 안 보인다.**
      다음 판에서 «늘리는 방식» 과 «재단선 보이기» 를 같이 손본다.
      `model.ts` 의 `BLEED_ENABLED` 를 `true` 로 되돌리면 계산·UI 가 그대로 살아난다.
      지금은 **맞붙이기 하나뿐** — 집에서 잘라 쓰는 데는 그게 맞다
- [x] **도련은 카드가 아니라 인쇄물의 속성** — 편집 캔버스에는 도련이 없다 (63×88 그 자체).
      도련은 **인쇄 미리보기에서 켜고 끄는 스위치**다 (`SheetSpec.bleed` / `bleedMode`):
      **없음(맞붙이기)** · **바깥 테두리만**(안쪽 이음매는 이웃이 도련 노릇 → A4 9장에서
      가운데엔 안 붙는다) · **조각마다**(인쇄소용). 붙일 변은 «실제로 깔린 조각들» 기준이라
      반만 찬 마지막 장도 맞는다. 재단선 자리는 도련 방식과 무관하게 고정

**완료 기준** — 가정용 양면 인쇄로 앞뒤가 맞물린다. **자로 재서 치수가 맞는다.**
→ ⚠️ **아직 한 번도 실물로 안 뽑아봤다.** PDF 안에서 잰 것뿐이다. ← **다음 할 일**

## v0.4 — 쓸 만한 편집기 (대부분 앞당겨 함)

- [x] **투명한 곳을 누르면 뒤 레이어가 잡힌다** (`core/hit.ts`) — 상자가 아니라 «칠해진 화소» 로 고른다.
      그림은 알파맵 축소본(`store/alpha.ts`), 글자는 렌더된 줄 상자, 그늘은 방향축 알파,
      빈 테두리 도형은 선 위만. 고른 레이어는 자기보다 **아래** 것보다 우선 —
      투명한 부분을 잡아도 계속 끌 수 있고, 위에 덮인 건 그대로 잡힌다.
      호버 테두리도 같은 판정을 쓰므로 **테두리가 곧 «누르면 잡힐 것»** 이다
- [x] 실행 취소 / 다시 실행 — 끌기 한 번이 기록 한 칸
- [x] 정렬 가이드 · 스냅 — 판 기준은 실선, 다른 레이어 기준은 점선+이름표. Alt 로 끔
- [ ] 여러 개 선택
- [ ] 복사·붙여넣기
- [x] 레이어 숨김 / 삭제  · [ ] 잠금 UI (모델엔 있고 버튼이 없다)
- [x] 이미지 끌어다 놓기로 추가
- [x] 색 고르개 — 팔레트 + **이 작업에 쓴 색** 자동 수집
- [x] **글꼴 직접 넣기** — ttf·otf·woff·woff2 를 불러오면 `FontFace` 로 등록된다.
      파일은 IndexedDB(그림과 같은 자리), `project.json` 에는 `fonts[]` 로 **이름만** 남는다.
      다른 브라우저에서 열면 «없는 글꼴» 을 알려주고, 이름을 바꾸면 쓰던 글자도 따라간다.
      굵기는 **합성 허용(`400`)이 기본** — 굵기 하나짜리 글꼴도 굵게가 먹는다.
      진짜 Bold 파일을 따로 넣었다면 글꼴 목록의 «굵게» 를 꺼서 합성을 막는다.
      인쇄는 같은 문서라 PDF 에 그대로 박힌다 (`document.fonts.ready` 를 기다린 뒤 인쇄)
- [x] 덱 복제 (틀까지 복사, 뒷면도 같이) · [ ] 앞면 디자인을 뒷면으로 복제
- [x] 덱 순서 이동 — **끌어서** (레이어 목록과 같은 방식). 어느 덱을 먼저 보느냐가 작업 순서다
- [x] 프로젝트 이름 — 머리말에서 바로 고친다. 저장 파일 이름이 된다
- [x] **안내선 없앰** — 판의 크기가 곧 카드의 크기라 그 위에 선을 더 그어도
      알려주는 게 없다. 안전 여백(`PieceSize.safe`)도 같이 뺐다.
      원형 조각은 **판 자체가 둥글다.** 여백 자리는 끄는 동안 스냅 가이드로만 나타난다
- [x] **키워드** — 규칙 용어(«돌진», «선제»)를 한 번 적어두면 **모든 카드에서** 자동으로
      칩·진하게·색으로 그려진다. 카드 글에는 낱말만 쓴다. 렌더 코드 한 곳에서 처리해
      화면과 인쇄가 같다. 긴 낱말이 먼저 걸린다(«연속 돌진» vs «돌진»).
      프로젝트 전체가 공유 — 규칙 용어는 덱 하나의 것이 아니다
- [x] **눈금자** — 판의 위·왼쪽에 mm 눈금. 0 은 카드의 왼쪽 위 모서리(= 재단선).
      확대 배율에 따라 1/5/10mm 로 간격이 바뀌고, **고른 레이어가 차지하는 구간**과
      마우스 자리가 눈금 위에 표시된다. 머리말의 «눈금자» 스위치로 끄고 켠다(기억됨)
- [x] **화면 실물 크기 보정** — 브라우저의 `mm` 은 물리 단위가 아니다. CSS 는 1인치를
      무조건 96px 로 «정해» 두므로, 실제 120dpi 모니터에서는 «100%» 가 자로 재면 80% 다.
      프로그램이 알아낼 방법이 없어 **사람이 한 번 재준다** (자 100mm 또는 신용카드 85.6mm).
      보정값은 이 브라우저에 남는다. ⚠️ **인쇄 출력에는 영향 없음** — 인쇄는 진짜 mm 로 나간다
- [x] Ctrl + 클릭 = 투명 무시하고 잡기 — 아주 옅은 그늘처럼 «보이는데 안 잡히는» 것의 탈출구
- [x] **그늘이 통째로 사라지던 문제** — CSS 는 `linear-gradient` 가 조금이라도 어긋나면
      선언 전체를 버린다. 방향을 타이핑하는 중(`to b`)이나 Enter 로 줄을 추가한 순간
      그늘이 화면에서 없어졌다. 이제 알아들을 수 있는 데까지 살려 그리고
      (`gradientCss`), 정말 못 그리면 속성 패널이 그 이유를 말해준다.
      색 단계 입력칸이 **빈 줄을 지우던 버그**도 같이 고침 — 줄을 추가할 수가 없었다

## v0.5 — sub3 전체를 이걸로 다시

- [ ] 카드 11덱 + 토큰 4종을 편집기로 재작성
- [x] ~~기존 데이터(TSV) 가져오기~~ — 그 자료가 문서와 어긋나 안 쓰기로 했고 폴더도 지웠다.
      대신 `example/build.mjs` 가 **문서를 옮겨적어** project.json 을 만든다
- [ ] 실제 인쇄 검증

**완료 기준** — sub3 구성품 전부를 handeck 으로 만들고 **실물로 뽑아 확인**한 상태.
이게 진짜 합격선이다.

> 원래 합격선은 «`sub3/nandeck/` 을 지워도 되는 상태» 였는데, **2026-08-03 에 먼저 지웠다.**
> 그 PDF 들도 실물 인쇄로 검증된 적이 없어 지키고 있을 이유가 없었다.
> 그림은 `sub3-chars/` · `sub3-omens/` 에 원본이 그대로 있다 (내용 해시로 확인하고 지웠다).

## v0.6 — 룰북 · 보드판

- [x] 여러 쪽 문서 (룰북) — **4차에서 함.** 아래 참조
- [x] 대형 인쇄물 + 타일링 (보드판) — **3차에서 함.** 위 참조
- [x] 카드와 테마 공유 — 키워드·글꼴·색은 프로젝트 전체가 공유한다 (보드도 같은 것을 쓴다)

## v0.7 — 다른 출력 (PPT 는 사용자가 초기부터 요청한 것)

- [x] **PNG** — 지금 보는 카드 한 장, 또는 덱 전체를 격자로. 96/150/300/600dpi.
      라이브러리 없이 **SVG `foreignObject` → canvas** 로 간다. 그리는 코드는
      화면·인쇄와 같은 `Piece` 하나다. SVG 안은 바깥과 안 이어져 있어서
      **그림은 `data:` 로, 글꼴은 `@font-face` 로 직접 심어야 한다** —
      빠뜨려도 에러가 안 나고 빈 칸·대체 글꼴로 조용히 나오므로 특히 주의.
      `react-dom/server` 가 무거워(+75KB) 창을 열 때 받도록 떼어놨다
- [ ] PPTX (PPT 로 받는 업체용)
- [x] 공유용 묶음 내보내기 (이미지 포함 한 덩어리) — **2차에서 앞당겨 함.** 위 참조

## v1.0 — 공개

- [ ] 저장 포맷 문서화 + 버전 정책
- [ ] 예제 프로젝트
- [ ] 문서 ko + en
- [ ] CI, npm 배포, MIT
- [ ] 이름 확정

---

## 비목표

- **호스팅형 SaaS** — 로컬로 충분한지 먼저 본다
- **자체 조판 엔진** — 브라우저에 맡긴다
- **일반 그래픽 편집기** — 포토샵을 만들지 않는다. 구성품 배치에 필요한 것만
- **게임 규칙·밸런싱**

---

## 이전 시도에서 배운 것

전부 버렸지만 기록은 남긴다. 같은 실수를 두 번 하지 않기 위해서다.

1. **제품 모양을 먼저 정해야 한다.** 엔진부터 만들면 엔진에 맞는 제품이 나온다.
   «CLI + 템플릿» 으로 시작했더니 결국 nanDECK 이 됐다.
2. **«웹 기반» 을 조판 엔진 이야기로 바꿔 읽지 말 것.** 사용자가 말한 건 UI 였다.
3. 인쇄 실측은 [`docs/print-findings.md`](docs/print-findings.md) 에 있다. 다시 재지 말 것.
