# 아키텍처

## 0. 모양 — 서버 없는 웹앱

```
                      브라우저 하나
  ┌─────────────────────────────────────────────────┐
  │  편집기 (React)                                  │
  │    캔버스 · 레이어 · 인스턴스 · 인쇄 설정          │
  ├─────────────────────────────────────────────────┤
  │  core   컴포넌트+인스턴스 → HTML · 면付 · 수량     │
  ├──────────────────────┬──────────────────────────┤
  │  IndexedDB (이미지)   │  파일 시스템 (project.json)│
  └──────────────────────┴──────────────────────────┘
                      ↓ 내보내기
              PDF · PNG · PPTX  (전부 브라우저에서)
```

설치도 계정도 서버도 없다. URL 하나 열면 끝이고, GitHub Pages 로 배포할 수 있다.
파일은 File System Access API 로 **사용자 디스크에 직접** 읽고 쓴다
(Chrome·Edge. Firefox·Safari 는 다운로드/업로드 폴백).

**폰트는 번들한다.** 시스템 폰트에 기대면 남에게 프로젝트를 줬을 때 다른 글꼴로 보인다.
오픈 라이선스 한글 폰트를 앱에 넣어 «보이는 그대로» 를 보장한다.

> **미해결 — 브라우저에서 인쇄용 PDF 를 어떻게 뽑나.**
> 이전 설계는 이것 때문에 로컬 서버(Chromium)를 뒀다. 무서버로 가려면 §3 의
> 세 방법 중 하나가 성립해야 하고, **아직 검증 전이다.** 짓기 전에 스파이크로 잰다.

**화면과 인쇄가 같은 코드를 쓴다.** 편집기가 보여주는 것과 PDF 가 갈라지지 않는다.

---

## 1. 핵심 모델 — 컴포넌트와 인스턴스

카드를 만드는 실제 과정은 이렇다.

```
  베이스 컴포넌트 (카드 한 종류의 «틀»)          인스턴스 (실제 카드들)
  ┌────────────────────────┐
  │ 4  제목      T  ← 다름  │        ┌──────┐ ┌──────┐ ┌──────┐
  │ 3  본문      T  ← 다름  │  ───▶  │선혈의│ │칼날 비│ │돌림병│  …
  │ 2  그늘      ▤  (공통)  │        │  비  │ │      │ │      │
  │ 1  일러스트  🖼 ← 다름  │        └──────┘ └──────┘ └──────┘
  │ 0  바탕      ▭  (공통)  │
  └────────────────────────┘
```

**레이어는 기본적으로 공통이다.** 그중 «카드마다 달라야 하는» 것만 표시한다.
그 표시된 레이어가 인스턴스에서 편집 가능한 자리가 된다.

이 방향이 중요한 이유:

- **사용자가 배울 문법이 없다.** `{{ name }}` 같은 걸 타이핑하지 않는다.
  베이스에서 텍스트 레이어를 클릭하고 «카드마다 다르게» 를 켜면 끝이다.
- **틀을 나중에 고쳐도 전부 따라온다.** 테두리 색을 바꾸면 10장이 같이 바뀐다.
  단순 복사였다면 10장을 다 고쳐야 한다 — 그게 nanDECK 이 피하려던 고통이다.
- 카드 10장이 전부 다른 경우(재앙)와, 7종을 100장 찍는 경우(능력)를 같은 모델로 다룬다.

```jsonc
// project.json
{
  "handeck": 1,
  "name": "삼류 악녀는 싫어!",

  "components": {
    "omen": {
      "size": { "w": 63, "h": 88, "bleed": 3, "shape": "rect" },
      "background": "#FFFDFA",
      "layers": [
        // override 가 없으면 «공통» — 모든 인스턴스가 이걸 그대로 쓴다
        { "id": "veil", "kind": "gradient", "x": 0, "y": 0, "w": 69, "h": 58,
          "direction": "to bottom",
          "stops": ["rgba(20,18,26,.72)", "rgba(255,253,250,0) 58%", "#FFFDFA"] },
        { "id": "kicker", "kind": "text", "x": 6, "y": 4, "w": 30, "h": 6,
          "text": "재앙", "size": 7.5, "color": "#D8B976" },

        // override 가 있으면 인스턴스마다 값을 갖는다
        { "id": "art",   "kind": "image", "x": 0, "y": 0, "w": 69, "h": 52,
          "fit": "cover", "override": "image" },
        { "id": "title", "kind": "text",  "x": 5, "y": 38, "w": 59, "h": 10,
          "size": 17, "weight": 700, "color": "#571026", "align": "center",
          "override": "text" },
        { "id": "body",  "kind": "text",  "x": 6, "y": 58, "w": 57, "h": 20,
          "size": 7.5, "align": "left", "override": "text" }
      ]
    }
  },

  "decks": [
    {
      "id": "omens",
      "component": "omen",
      "back": "dark-back",              // 다른 컴포넌트를 뒷면으로
      "sheet": { "size": "A4", "margin": 10, "marks": "crop" },
      "duplex": "long",
      "expect": 10,                     // 어긋나면 경고
      "instances": [
        { "id": "i1", "qty": 1, "values": {
            "art": "asset:9f2c1a", "title": "선혈의 비",
            "body": "주사위를 굴려 나온 지역에…" } },
        { "id": "i2", "qty": 1, "values": {
            "art": "asset:4b71de", "title": "칼날 비",
            "body": "동·서·남·북·아카데미에…" } }
      ]
    }
  ]
}
```

### 두 가지 편집 방식, 하나의 데이터

`instances` 배열은 그대로 **표**이기도 하다. 열 = 오버라이드 가능한 레이어, 행 = 인스턴스.

- **캔버스 편집** — 카드를 보면서 글자·그림을 직접 고친다 (기본)
- **표 편집** — 100줄을 스프레드시트에서 붙여넣는다 (대량 작업)

둘이 같은 데이터를 본다. CSV 를 붙여넣으면 열 이름을 오버라이드 레이어에 맞춰 인스턴스가 생긴다.

### 레이어 종류

| kind | 쓰임 | override |
| --- | --- | --- |
| `image` | 일러스트 · 아이콘 | `"image"` — 인스턴스마다 다른 그림 |
| `text` | 글자 | `"text"` — 인스턴스마다 다른 내용 |
| `md` | **글 덩어리** (룰북 본문) — 소제목·목록·표가 섞인 문서 | `"text"` — 쪽마다 다른 원고 |
| `rect` | 상자 · 테두리 · 원(`radius` 를 절반으로) | (공통) |
| `gradient` | 글자가 그림 위에서 읽히게 하는 그늘 | (공통) |
| `html` | 탈출구. 레이어로 안 되는 건 CSS 직접 | (공통) |

`md` 만 성격이 다르다. 나머지는 «상자 하나에 한 가지» 인데 이건 **상자 하나에 문서 한 덩어리**다.
쪼개서 레이어로 만들면 문장 하나를 고칠 때마다 아래 상자를 전부 다시 잡아야 하기 때문이다.
안쪽 치수는 전부 **본문 글씨 기준(em)** 이라 크기 하나만 줄이면 소제목·여백·표가 같이 줄어든다.

### 세 가지 인쇄물 — 덱 · 보드 · 룰북

같은 «틀(컴포넌트)» 을 쓰지만 **종이에 앉히는 문제가 서로 다르다.** 그래서 조판이 셋이다.

| | 무엇 | 종이에 앉히는 문제 | 코드 |
| --- | --- | --- | --- |
| **덱** | 작은 조각 여러 장 | 격자로 깔고 **자른다** — 도련·재단선·양면 맞물림 | `core/impose.ts` |
| **보드** | 한 장짜리 큰 인쇄물 | 나눠 뽑아 **이어 붙인다** — 겹침(풀칠 여유)·붙일 자리 | `core/tile.ts` |
| **룰북** | 순서 있는 여러 쪽 | **접거나 묶는다** — 쪽 순서·앞뒤 짝·백지 채우기 | `core/booklet.ts` |

편집 화면은 셋 다 같다 (`componentId()` 만 탭에 따라 갈린다). 인스턴스도 같은 자리를 쓴다 —
덱은 카드, 보드는 없음, 룰북은 **쪽**이다. 그래서 «쪽마다 다른 본문» 이 «카드마다 다른 문구» 와
정확히 같은 길로 흐른다.

중철(`saddle`)이 이 중 유일하게 «계산해야 아는» 것이다. 8쪽짜리는 종이에
`8|1` / `2|7` / `6|3` / `4|5` 로 깔린다 — 손으로 만들면 8쪽까지는 되고 16쪽부터 반드시 틀린다.

### 원고 → 룰북

```
마크다운 (`---` 로 나뉜 쪽)  ──parseRulebookSource──▶  { title, pages[] }
                                    │
                                    └──buildRulebook──▶  쪽 틀(Component) + 쪽 목록(Instance[])
```

**가져오기가 곧 첫 편집이다.** 20쪽짜리 문서를 상자부터 놓아 만들게 하면 이 도구가
없애려던 루프(고치고·빌드하고·확인하고)가 그대로 돌아온다. 규칙 문서는 어차피
마크다운으로 쓰고 있으니 그게 그대로 입력이 된다.

**겹침이 기본이다.** 전면 일러스트 → 그늘 → 글자 순으로 쌓는 게 기본 구성이다.
좌표는 전부 mm.

### 이미지는 IndexedDB 에

브라우저 안에서 도는 앱이라 이미지를 파일 경로로 들 수 없다.

- 업로드한 이미지를 **내용 해시**로 키를 만들어 IndexedDB 에 blob 으로 저장한다
- `project.json` 은 `"asset:9f2c1a"` 처럼 **id 만** 참조한다 — 파일이 커도 JSON 은 가볍다
- 같은 그림을 여러 번 올려도 해시가 같아 한 번만 저장된다
- 공유할 때는 **묶음 내보내기** 로 `project.json` + 쓰인 이미지를 zip 으로 뽑는다.
  받는 쪽이 열면 다시 IndexedDB 로 들어간다.

---

## 2. 계층

```
ui/      React 편집기 — 캔버스 · 레이어 · 인스턴스 · 인쇄
core/    컴포넌트+인스턴스 → DOM · 면付 · 수량 확장   (UI 도 export 도 이걸 쓴다)
store/   IndexedDB (이미지) · 파일 열기·저장
export/  pdf · png · pptx  (전부 브라우저)
```

`core` 가 화면과 내보내기 양쪽에 쓰이는 게 요점이다.
**렌더 코드가 한 곳뿐이라** 편집기와 PDF 가 갈라질 수 없다.

---

## 3. 인쇄 정확도 — 여기가 유일한 미해결 지점

[`docs/print-findings.md`](docs/print-findings.md) 는 **서버에서 Chromium 을 띄웠을 때**의
실측이다. 여전히 유효한 사실:

- Chromium 의 PDF **페이지 상자는 1/300in 격자에 스냅**된다 (최대 0.18mm 오차)
- 칠해지는 박스는 정수 CSS 픽셀로 스냅된다 (±0.13mm, **절대 오차 — 크기에 비례 안 함**)
  → 배경은 블리드 밖까지 넘겨 그린다. 편집기가 기본으로 그렇게 만든다.
- 시트에 앉힐 때 **배율을 주지 않는다.** 주면 스냅 오차가 번져 원이 타원이 된다.
- 양면은 격자 좌표 기준으로 열을 반전한다. 마지막 시트가 덜 차도 마찬가지.

브라우저 안에서 PDF 를 만드는 길은 셋이고, 성격이 다르다.

| 방법 | 글자 | 자동화 | 위험 |
| --- | --- | --- | --- |
| 브라우저 인쇄 대화상자 | **벡터** | ✗ 사용자가 «여백 없음·배율 100%» 설정 | 설정 틀리면 치수가 어긋남 |
| DOM → 래스터 → pdf-lib | 래스터 | ✓ | 브라우저 내 래스터화가 불안정 (폰트·CORS) |
| DOM 에서 **줄 위치만 재서** pdf-lib 로 벡터 그리기 | **벡터** | ✓ | 구현이 제일 복잡 |

세 번째가 유력하다 — 줄바꿈은 브라우저가 하고(한글 규칙 유지), 그 결과 좌표만 읽어
pdf-lib 로 벡터 텍스트를 찍는다. 원칙 3 을 안 깨면서 서버도 없앤다.

**셋 다 재보고 정한다.** 결과가 나쁘면 «웹앱 기본 + 선택적 로컬 헬퍼» 로 물러선다.

---

## 4. 스택

- **편집기**: React + TypeScript + Vite, 상태는 Zustand
- **저장**: File System Access API (폴백: 다운로드/업로드) · 이미지는 IndexedDB
- **PDF**: pdf-lib · **PPTX**: pptxgenjs · 전부 브라우저에서
- **폰트**: 오픈 라이선스 한글 폰트 번들
- 배포: 정적 사이트 (GitHub Pages)

## 5. 안 하는 것

- **계정·클라우드 저장** — 파일은 사용자 디스크에 남는다. 공유는 파일로 한다.
- **자체 조판 엔진** — 원칙 3 위반.
- **CMYK** — Chromium 이 RGB 만 낸다. 색 정확도가 중요하면 프리프레스 도구가 맞다.
- **일반 그래픽 편집기** — 포토샵을 만들지 않는다. 구성품 배치에 필요한 것만.
- **게임 규칙·밸런싱** — 구성품 도구지 게임 엔진이 아니다.
